---
title: Helen Chen's Data550 Final Project Outline
author: Hua Xuan (Helen) Chen
output: github_document
---

## Introduction

The dataset I used for my final project is from Kaggle
(https://www.kaggle.com/datasets/aliiihussain/sports-footwear-sales-and-consumer-behavior?resource=download), and it contains consumer and sales data on shoe purchases from 2018 to 2026. The author of the dataset is Ali Hussain. The dataset contains 30,000 rows and 18 columns, and the columns include the shoe brand (i.e. Nike, Adidas, Reebok, etc.), color of the shoe, country where the sale occurred, the discount applied on the sale (in %), and more. 

There are three main folders in the project directory: output, data, and code. The output folder stores the outputs generated by the R scripts in the code folder; they are in rds and png formats. The data folder contains the shoe data in csv format. The project folder also contains a Report.Rmd, a .gitignore file, a Makefile, a Dockerfile, and the README. The renv folder and renv.lock are to ensure that you can run the code with the same packages and with the same versions of those packages. 

You may enter "make final_report/report.html" in the terminal, which will generate the final report in html format, and it will be in the "final_report" folder. The contents of the report are: EDA table and 2 boxplots, coefficients from group LASSO for feature selection in linear regression to find the most important predictors of revenue, and results from the final linear regression model in a table.

After you fork and clone this repo to your computer, navigate to that directory in your terminal using the cd command. To ensure you have the necessary packages and versions of those packages installed to run my code, please do the following:
1. Navigate to your console, and set the working directory to the project directory using setwd()
2. Next, type in and enter source("renv/activate.R"), which is able to check that renv's installed
3. To install the packages in the renv lockfile, type in renv::restore() in the console. Type in y and press enter to proceed.

Alternatively, you may enter "make install" in the terminal, which ensures that your library is synchronized with the renv lockfile. The make rule restores the package library.

## Code description:

`code/make_table.R`

  - This code creates the table that shows the total shoe units sold and total revenue (in USD) by country, in increasing order of total revenue.

  - Saves the table as a `.rds` object in `output/` folder

`code/render_report.R`

  - Renders `Report.Rmd`
  
`code/make_boxplots.R`

  - This code creates two boxplots:
  
  - First boxplot shows the distribution of revenue for each of the shoe brands in the dataset, in increasing order of median revenue
  
  - Second boxplot shows the distribution of revenue for each of the shoe categories, in increasing order of median revenue
  
  - Saves the boxplots as `.png` files in `output/` folder
  
`code/model.R`

  - Saves the matrix of coefficients from group LASSO output as a `.rds` object in `output/` folder
  
  - Also, I re-ran a linear regression model with all the variables that had non-zero coefficients from group LASSO
  
  - The output from the model above is saved as a `.rds` object in `output/` folder
  
`Report.Rmd`

  - Displays a summary table, 2 boxplots, coefficients from group LASSO, and summary table of my final linear regression model

